pipeline {
    agent any
    environment {
        staging_server="139.59.80.16"
       
    }

    stages {
        stage('Deploy to Remote') {
            steps {
                sh "scp -r ${WORKSPACE}/* root@${staging_server}:/var/www/html/phpwebapp/"
            }
        }
    }
}
